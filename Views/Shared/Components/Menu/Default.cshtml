@model IEnumerable<Category>

<div id="menu">
        <ul class="list-group">
            @foreach (var category in Model)
            {
                <li>
                    <a href="/">@category.Name</a>
                </li>    
            }
        </ul>
</div>